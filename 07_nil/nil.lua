--[[
    Nil is a type with a single value, nil,
    whose property is to be different from ant other value.

    As we have seen,
    a global variable has a nil value by default,
    before its first assignment,
    and we can assign nil to a global variable to delete it.
]]

print(a)
print(type(a))
a = 10
print(a)
a = nil
print(a)